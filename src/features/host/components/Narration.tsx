import { cn } from "@home/utils/css/classNames";

export const Narration = (props: { className: string }) => {
  return (
    <div className={cn(props.className, "wrap-break-word")}>narration...</div>
  );
};
